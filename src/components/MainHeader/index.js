import React from 'react';
import styles from './styles.module.css';

const MainHeader = () => {
    return (
        <header className={styles.mainHeader}>
            <h1 className={styles.title}>React Toasts</h1>
        </header>
    );
}

export default MainHeader;
